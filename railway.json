{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader && npm install --no-audit --no-fund && npm run build"
    },
    "deploy": {
        "preDeployCommand": "php artisan migrate --force",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}
